FROM node:18-alpine

WORKDIR /app

# Set CI environment to prevent interactive mode
ENV CI=true

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install --legacy-peer-deps

# Copy source code and tests
COPY . .

# Default command (overridden by docker-compose)
CMD ["npx", "vitest", "run", "--coverage"]

