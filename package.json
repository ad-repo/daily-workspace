{
  "name": "track-the-thing",
  "version": "1.0.0",
  "description": "Track the Thing - Daily notes and task management",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:all": "cd backend && ./run_tests.sh && cd ../frontend && ./run_tests.sh && cd .. && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1"
  }
}

